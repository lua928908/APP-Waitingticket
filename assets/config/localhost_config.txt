# localhost:8080 연결 테스트 설정
# 이 파일은 개발 환경에서 localhost 서버 연결을 위한 설정입니다.

# 서버 URL 설정
SERVER_URL=http://localhost:8080
BASE_PATH=/pos/posWaitingRegistView

# 연결 테스트 설정
CONNECTION_TIMEOUT=10000
READ_TIMEOUT=30000

# 디버그 모드 설정
DEBUG_MODE=true
LOG_LEVEL=verbose

# 웹뷰 설정
WEBVIEW_ZOOM_ENABLED=false
WEBVIEW_JAVASCRIPT_ENABLED=true
WEBVIEW_CACHE_ENABLED=true

# 네트워크 설정
ALLOW_HTTP_TRAFFIC=true
ALLOW_LOCALHOST=true
ALLOW_SELF_SIGNED_CERTIFICATES=true
